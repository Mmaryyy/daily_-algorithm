//배열을 입력받아 차례대로 
//배열의 첫 요소와 마지막 요소를 
//키와 값으로 하는 객체를 리턴해야 합니다.

function transformFirstAndLast(arr) {
    // TODO: 여기에 코드를 작성합니다.
    // 배열의 첫 요소를 가져온다
    // 배열의 마지막 요소를 가져온다
    // 빈 객체를 생성한다
    // 첫 요소를 객체의 키로, 마지막 요소를 객체의 값으로 할당한다

    let resultObj = {}
    let keyName = arr[0]
    let keyValue = arr[arr.length - 1]

    return resultObj.keyName = keyValue
  }

